# Decision Lens API
#
# No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
#
# OpenAPI spec version: 1.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' Portfolios Class
#'
#' @field href 
#' @field offset 
#' @field limit 
#' @field size 
#' @field items 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Portfolios <- R6::R6Class(
  'Portfolios',
  public = list(
    `href` = NULL,
    `offset` = NULL,
    `limit` = NULL,
    `size` = NULL,
    `items` = NULL,
    initialize = function(`href`, `offset`, `limit`, `size`, `items`){
      if (!missing(`href`)) {
        stopifnot(is.character(`href`), length(`href`) == 1)
        self$`href` <- `href`
      }
      if (!missing(`offset`)) {
        stopifnot(is.numeric(`offset`), length(`offset`) == 1)
        self$`offset` <- `offset`
      }
      if (!missing(`limit`)) {
        stopifnot(is.numeric(`limit`), length(`limit`) == 1)
        self$`limit` <- `limit`
      }
      if (!missing(`size`)) {
        stopifnot(is.numeric(`size`), length(`size`) == 1)
        self$`size` <- `size`
      }
      if (!missing(`items`)) {
        stopifnot(is.list(`items`), length(`items`) != 0)
        lapply(`items`, function(x) stopifnot(R6::is.R6(x)))
        self$`items` <- `items`
      }
    },
    toJSON = function() {
      PortfoliosObject <- list()
      if (!is.null(self$`href`)) {
        PortfoliosObject[['href']] <- self$`href`
      }
      if (!is.null(self$`offset`)) {
        PortfoliosObject[['offset']] <- self$`offset`
      }
      if (!is.null(self$`limit`)) {
        PortfoliosObject[['limit']] <- self$`limit`
      }
      if (!is.null(self$`size`)) {
        PortfoliosObject[['size']] <- self$`size`
      }
      if (!is.null(self$`items`)) {
        PortfoliosObject[['items']] <- lapply(self$`items`, function(x) x$toJSON())
      }

      PortfoliosObject
    },
    fromJSON = function(PortfoliosJson) {
      PortfoliosObject <- dlensFromJSON(PortfoliosJson)
      if (!is.null(PortfoliosObject$`href`)) {
        self$`href` <- PortfoliosObject$`href`
      }
      if (!is.null(PortfoliosObject$`offset`)) {
        self$`offset` <- PortfoliosObject$`offset`
      }
      if (!is.null(PortfoliosObject$`limit`)) {
        self$`limit` <- PortfoliosObject$`limit`
      }
      if (!is.null(PortfoliosObject$`size`)) {
        self$`size` <- PortfoliosObject$`size`
      }
      if (!is.null(PortfoliosObject$`items`)) {
        self$`items` <- lapply(PortfoliosObject$`items`, function(x) {
          itemsObject <- Portfolio$new()
          itemsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          itemsObject
        })
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "href": %s,
           "offset": %d,
           "limit": %d,
           "size": %d,
           "items": [%s]
        }',
        self$`href`,
        self$`offset`,
        self$`limit`,
        self$`size`,
        lapply(self$`items`, function(x) paste(x$toJSON(), sep=","))
      )
    },
    fromJSONString = function(PortfoliosJson) {
      PortfoliosObject <- dlensFromJSON(PortfoliosJson)
      self$`href` <- PortfoliosObject$`href`
      self$`offset` <- PortfoliosObject$`offset`
      self$`limit` <- PortfoliosObject$`limit`
      self$`size` <- PortfoliosObject$`size`
      self$`items` <- lapply(PortfoliosObject$`items`, function(x) Portfolio$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
    }
  )
)
