# Decision Lens API
#
# No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
#
# OpenAPI spec version: 1.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' PortfolioPlans Class
#'
#' @field href 
#' @field offset 
#' @field limit 
#' @field size 
#' @field items 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PortfolioPlans <- R6::R6Class(
  'PortfolioPlans',
  public = list(
    `href` = NULL,
    `offset` = NULL,
    `limit` = NULL,
    `size` = NULL,
    `items` = NULL,
    initialize = function(`href`, `offset`, `limit`, `size`, `items`){
      if (!missing(`href`)) {
        stopifnot(is.character(`href`), length(`href`) == 1)
        self$`href` <- `href`
      }
      if (!missing(`offset`)) {
        stopifnot(is.numeric(`offset`), length(`offset`) == 1)
        self$`offset` <- `offset`
      }
      if (!missing(`limit`)) {
        stopifnot(is.numeric(`limit`), length(`limit`) == 1)
        self$`limit` <- `limit`
      }
      if (!missing(`size`)) {
        stopifnot(is.numeric(`size`), length(`size`) == 1)
        self$`size` <- `size`
      }
      if (!missing(`items`)) {
        stopifnot(is.list(`items`), length(`items`) != 0)
        lapply(`items`, function(x) stopifnot(R6::is.R6(x)))
        self$`items` <- `items`
      }
    },
    toJSON = function() {
      PortfolioPlansObject <- list()
      if (!is.null(self$`href`)) {
        PortfolioPlansObject[['href']] <- self$`href`
      }
      if (!is.null(self$`offset`)) {
        PortfolioPlansObject[['offset']] <- self$`offset`
      }
      if (!is.null(self$`limit`)) {
        PortfolioPlansObject[['limit']] <- self$`limit`
      }
      if (!is.null(self$`size`)) {
        PortfolioPlansObject[['size']] <- self$`size`
      }
      if (!is.null(self$`items`)) {
        PortfolioPlansObject[['items']] <- lapply(self$`items`, function(x) x$toJSON())
      }

      PortfolioPlansObject
    },
    fromJSON = function(PortfolioPlansJson) {
      PortfolioPlansObject <- dlensFromJSON(PortfolioPlansJson)
      if (!is.null(PortfolioPlansObject$`href`)) {
        self$`href` <- PortfolioPlansObject$`href`
      }
      if (!is.null(PortfolioPlansObject$`offset`)) {
        self$`offset` <- PortfolioPlansObject$`offset`
      }
      if (!is.null(PortfolioPlansObject$`limit`)) {
        self$`limit` <- PortfolioPlansObject$`limit`
      }
      if (!is.null(PortfolioPlansObject$`size`)) {
        self$`size` <- PortfolioPlansObject$`size`
      }
      if (!is.null(PortfolioPlansObject$`items`)) {
        self$`items` <- lapply(PortfolioPlansObject$`items`, function(x) {
          itemsObject <- PortfolioPlan$new()
          itemsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          itemsObject
        })
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "href": %s,
           "offset": %d,
           "limit": %d,
           "size": %d,
           "items": [%s]
        }',
        self$`href`,
        self$`offset`,
        self$`limit`,
        self$`size`,
        lapply(self$`items`, function(x) paste(x$toJSON(), sep=","))
      )
    },
    fromJSONString = function(PortfolioPlansJson) {
      PortfolioPlansObject <- dlensFromJSON(PortfolioPlansJson)
      self$`href` <- PortfolioPlansObject$`href`
      self$`offset` <- PortfolioPlansObject$`offset`
      self$`limit` <- PortfolioPlansObject$`limit`
      self$`size` <- PortfolioPlansObject$`size`
      self$`items` <- lapply(PortfolioPlansObject$`items`, function(x) PortfolioPlan$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
    }
  )
)
