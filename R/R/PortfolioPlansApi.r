# Decision Lens API
#
# No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
#
# OpenAPI spec version: 1.0
#
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title PortfolioPlans operations
#' @description swagger.PortfolioPlans
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' add_portfolio_plan_users Add users to a portfolio plan.
#'
#'
#' apply_recommendations_for_portfolio_plan Apply recommendations of a portfolio plan.
#'
#'
#' create_portfolio_plan Create a portfolio plan
#'
#'
#' create_projects_in_portfolio_plan Create new projects
#'
#'
#' delete_portfolio_plan Deletes portfolio plan
#'
#'
#' export_portfolio_plan Export portfolio plan
#'
#'
#' get_budget_allocations_for_portfolio_plan Retrieves budget allocations for a portfolio plan.
#'
#'
#' get_minified_projects_for_portfolio_plan Retrieves minified projects contained within a portfolio plan.
#'
#'
#' get_portfolio_plan Retrieves portfolio plan
#'
#'
#' get_portfolio_plan_activities Retrieves portfolio plan activities log.
#'
#'
#' get_portfolio_plan_users Retrieves users in a portfolio plan.
#'
#'
#' get_portfolio_plans Retrieves portfolio plans
#'
#'
#' get_project_for_portfolio_plan Retrieves project contained within a portfolio plan.
#'
#'
#' get_project_portfolio_plans_for_portfolio_plan_and_portfolio Retrieves projects for portfolio plan
#'
#'
#' get_projects_for_portfolio_plan Retrieves projects contained within a portfolio plan. Possible expand paths - (items.fieldValues, contributingUserIds)
#'
#'
#' get_recommendations_for_portfolio_plan Retrieves recommendations for a portfolio plan.
#'
#'
#' get_schedule_for_portfolio_plan Retrieves schedule for portfolio plan. The schedule is stored in a new portfolio plan.
#'
#'
#' rebaseline_portfolio_plan Rebaseline a portfolio plan.
#'
#'
#' update_portfolio_plan Updates a portfolio plan utilizing JSON Patch Operations.
#'
#'
#' update_portfolio_plan_field_values Update field values in a portfolio plan
#'
#'
#' update_portfolio_plan_users Update users in a portfolio plan
#'
#' }
#'
#' @export
PortfolioPlansApi <- R6::R6Class(
  'PortfolioPlansApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    add_portfolio_plan_users = function(id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/portfolioPlans/{id}/users"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PortfolioPlanUser$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    apply_recommendations_for_portfolio_plan = function(id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/portfolioPlans/{id}/recommendations"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FieldValue$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    create_portfolio_plan = function(body, expand, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`expand`)) {
        queryParams['expand'] <- expand
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/portfolioPlans"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PortfolioPlan$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    create_projects_in_portfolio_plan = function(id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/portfolioPlans/{id}/projects"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Project$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_portfolio_plan = function(id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/portfolioPlans/{id}"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    export_portfolio_plan = function(id, export_type, export_format, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`export_type`)) {
        queryParams['exportType'] <- export_type
      }

      if (!missing(`export_format`)) {
        queryParams['exportFormat'] <- export_format
      }

      urlPath <- "/portfolioPlans/{id}/export"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_budget_allocations_for_portfolio_plan = function(id, start_time, end_time, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`start_time`)) {
        queryParams['startTime'] <- start_time
      }

      if (!missing(`end_time`)) {
        queryParams['endTime'] <- end_time
      }

      urlPath <- "/portfolioPlans/{id}/budgetAllocations"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- BudgetAllocations$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_minified_projects_for_portfolio_plan = function(id, expand, limit, offset, order_by, match, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`expand`)) {
        queryParams['expand'] <- expand
      }

      if (!missing(`limit`)) {
        queryParams['limit'] <- limit
      }

      if (!missing(`offset`)) {
        queryParams['offset'] <- offset
      }

      if (!missing(`order_by`)) {
        queryParams['orderBy'] <- order_by
      }

      if (!missing(`match`)) {
        queryParams['match'] <- match
      }

      urlPath <- "/portfolioPlans/{id}/projects/minify"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- MinifiedProjects$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_portfolio_plan = function(id, expand, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`expand`)) {
        queryParams['expand'] <- expand
      }

      urlPath <- "/portfolioPlans/{id}"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PortfolioPlan$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_portfolio_plan_activities = function(id, project_id, limit, offset, match, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`project_id`)) {
        queryParams['projectId'] <- project_id
      }

      if (!missing(`limit`)) {
        queryParams['limit'] <- limit
      }

      if (!missing(`offset`)) {
        queryParams['offset'] <- offset
      }

      if (!missing(`match`)) {
        queryParams['match'] <- match
      }

      urlPath <- "/portfolioPlans/{id}/activities"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Activities$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_portfolio_plan_users = function(id, limit, offset, order_by, match, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`limit`)) {
        queryParams['limit'] <- limit
      }

      if (!missing(`offset`)) {
        queryParams['offset'] <- offset
      }

      if (!missing(`order_by`)) {
        queryParams['orderBy'] <- order_by
      }

      if (!missing(`match`)) {
        queryParams['match'] <- match
      }

      urlPath <- "/portfolioPlans/{id}/users"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PortfolioPlanUsers$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_portfolio_plans = function(portfolio_id, expand, limit, offset, order_by, match, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`portfolio_id`)) {
        queryParams['portfolioId'] <- portfolio_id
      }

      if (!missing(`expand`)) {
        queryParams['expand'] <- expand
      }

      if (!missing(`limit`)) {
        queryParams['limit'] <- limit
      }

      if (!missing(`offset`)) {
        queryParams['offset'] <- offset
      }

      if (!missing(`order_by`)) {
        queryParams['orderBy'] <- order_by
      }

      if (!missing(`match`)) {
        queryParams['match'] <- match
      }

      urlPath <- "/portfolioPlans"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PortfolioPlans$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_project_for_portfolio_plan = function(portfolio_plan_id, project_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/portfolioPlans/{portfolioPlanId}/projects/{projectId}"
      if (!missing(`portfolio_plan_id`)) {
        urlPath <- gsub(paste0("\\{", "portfolioPlanId", "\\}"), `portfolio_plan_id`, urlPath)
      }

      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Project$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_project_portfolio_plans_for_portfolio_plan_and_portfolio = function(portfolio_id, portfolio_plan_id, position_from, position_to, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`position_from`)) {
        queryParams['positionFrom'] <- position_from
      }

      if (!missing(`position_to`)) {
        queryParams['positionTo'] <- position_to
      }

      urlPath <- "/portfolioPlans/{portfolioPlanId}/portfolios/{portfolioId}/projectPortfolioPlans"
      if (!missing(`portfolio_id`)) {
        urlPath <- gsub(paste0("\\{", "portfolioId", "\\}"), `portfolio_id`, urlPath)
      }

      if (!missing(`portfolio_plan_id`)) {
        urlPath <- gsub(paste0("\\{", "portfolioPlanId", "\\}"), `portfolio_plan_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Projects$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_projects_for_portfolio_plan = function(id, expand, limit, offset, order_by, match, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`expand`)) {
        queryParams['expand'] <- expand
      }

      if (!missing(`limit`)) {
        queryParams['limit'] <- limit
      }

      if (!missing(`offset`)) {
        queryParams['offset'] <- offset
      }

      if (!missing(`order_by`)) {
        queryParams['orderBy'] <- order_by
      }

      if (!missing(`match`)) {
        queryParams['match'] <- match
      }

      urlPath <- "/portfolioPlans/{id}/projects"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Projects$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_recommendations_for_portfolio_plan = function(id, start_date, end_date, plan_type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`start_date`)) {
        queryParams['startDate'] <- start_date
      }

      if (!missing(`end_date`)) {
        queryParams['endDate'] <- end_date
      }

      if (!missing(`plan_type`)) {
        queryParams['planType'] <- plan_type
      }

      urlPath <- "/portfolioPlans/{id}/recommendations"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Recommendations$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_schedule_for_portfolio_plan = function(id, start_date, end_date, tolerance_level_percentage, scheduling_criteria, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`start_date`)) {
        queryParams['startDate'] <- start_date
      }

      if (!missing(`end_date`)) {
        queryParams['endDate'] <- end_date
      }

      if (!missing(`tolerance_level_percentage`)) {
        queryParams['toleranceLevelPercentage'] <- tolerance_level_percentage
      }

      if (!missing(`scheduling_criteria`)) {
        queryParams['schedulingCriteria'] <- scheduling_criteria
      }

      urlPath <- "/portfolioPlans/{id}/schedule"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Recommendations$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    rebaseline_portfolio_plan = function(id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/portfolioPlans/{id}/rebaseline"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PortfolioPlan$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_portfolio_plan = function(id, params, body, expand,  ...){
      args <- list(...)
      queryParams <- params
      headerParams <- character()

      if (!missing(`expand`)) {
        queryParams['expand'] <- expand
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/portfolioPlans/{id}"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PortfolioPlan$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_portfolio_plan_field_values = function(planId, params, body, expand, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`expand`)) {
        queryParams['expand'] <- expand
      }
      
      if (!missing(`body`)) {
        #body <- `body`$toJSONString()
        body <- jsonlite::toJSON(body)
      } else {
        body <- ""
      }
      
      body = params

      urlPath <- "/portfolioPlans/{id}/fieldValues"
      urlPath <- gsub(paste0("\\{", "id", "\\}"), planId, urlPath)

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 httr::content_type("application/json-patch+json"),
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FieldValue$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_portfolio_plan_users = function(id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/portfolioPlans/{id}/users"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PortfolioPlanUser$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
